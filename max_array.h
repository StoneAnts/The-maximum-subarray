#ifndef MAX_ARRAY_H_INCLUDED
#define MAX_ARRAY_H_INCLUDED

int FIND_MAXIMUM_SUBARRAY(int a[], int low, int high);//传进去数组，数组的低位序号，高位序号，返回最大子数组
int FIND_MAX_CROSSING_SUBARRAY(int a[], int low, int mid, int high);//得到最大中子数组

#endif // MAX_ARRAY_H_INCLUDED
